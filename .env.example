# Copy to .env and fill in real values. NEVER commit .env.
#
# .env is excluded from coding assistants (.cursorignore / .codeiumignore /
# .aiexclude / .geminiignore, and .claude/settings.json deny rules) and from
# git. This .example file is the safe-to-commit template documenting which
# variables exist.

DATABASE_URL=
API_KEY=
