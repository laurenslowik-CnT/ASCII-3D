import type { StoryObj } from "@storybook/nextjs";
import { {{pascalCase name}} } from "./index";
import type { {{pascalCase name}}Props } from "./index";

const meta = {
  title: "Components/{{titleCase name}}",
  component: {{pascalCase name}},
  tags: ["autodocs"],
  parameters: {
    layout: "fullscreen",
    {{#if figmaLink}}
    design: {
      type: "figma",
      url: "{{ figmaLink }}",
    },
    {{/if}}
  },
  argTypes: {
    aColor: {
      control: {
        type: "color",
        presetColors: ["#ff0000", "#00ff00", "#0000ff"],
      },
    },
    anEnum: {
      control: "inline-radio",
      defaultValue: "bar",
      options: ["foo", "bar", "buzz"],
    },
    aNumber: {
      control: { type: "range", min: -1, max: 1500, step: 0.1 },
    },
    aBoolean: {
      control: "boolean",
    },
    aString: {
      control: "text",
    },
  },
};

export default meta;
type Story = StoryObj<typeof meta>;

// Use this as a quick-start for your default properties.
// you can add and remove items to this, or delete it if you like
const {{constantCase name}}_PROPS_EXAMPLE: {{pascalCase name}}Props = {
  aBoolean: false,
  aColor: "green",
  anEnum: "baz",
  aNumber: 42,
  aString: "a title",
};

export const Basic: Story = {
  render: (props: {{pascalCase name}}Props) => {
    // you can take in anything you want from "args" down there
    // and modify it here in this function before sending it out again.
    // This is a great way to customize and tweak your stories.
    return <{{pascalCase name}} {...props} />;
  },
  args: {
    ...{{constantCase name}}_PROPS_EXAMPLE,
  },
};
